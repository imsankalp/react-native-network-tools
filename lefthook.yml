no_tty: true

pre-commit:
  parallel: false
  commands:
    lint:
      glob: '**/*.{js,jsx,ts,tsx}'
      run: yarn eslint {staged_files}
    types:
      glob: '**/*.{js,jsx,ts,tsx}'
      run: yarn tsc
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: yarn commitlint --edit
